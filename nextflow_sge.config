process {
        executor='sge'
        queue = 'hubbioit'
        penv = 'thread'
        scratch = true
        clusterOptions = '-S /bin/bash'
}

executor {
        name = 'sge'
        queueSize = 600
}
